# Session Log — v1.6.1 Changelog/History Update

**Timestamp:** 2026-08-03T08:48:58Z

Cut the `[1.6.1] - 2026-08-03` release section in `.github/CHANGELOG.md` and `.github/HISTORY.md`, bundling three pieces of work into one release entry:

1. **ADR-0026 SQLite-backed resolved-model cache** — new `CacheService`/`CacheController`, `strata cache warm/status/clear/export` CLI group, auto-warm integration on `build run`/`build plan`/`policy check` (`--no-cache-warm` opt-out), and a VS Code background cache warmer (file-watch + startup, status bar indicator).
2. **`deploy run`/`deploy destroy` exit-code fix (ADR-0004)** — schema/cross-reference validation failures now correctly exit 3 instead of 1; missing/unresolvable deployment files now exit 2 via `click.UsageError`.
3. **Security fix carried over from the prior session** — `SecretStoreUnavailableError` contract across Infisical/HashiCorp Vault+OpenBao/Bitwarden/Azure Key Vault so store outages are no longer conflated with "not found", plus pre-flight availability checks for secret stores and provisioners before a deploy proceeds.

A fresh empty `## [Unreleased]` heading was restored above the new `[1.6.1]` section in both files. Branch: `release/v1.6.1` (tag `v1.6.1` is provisional per `scripts/Release.ps1`'s workflow — not touched by this session).
