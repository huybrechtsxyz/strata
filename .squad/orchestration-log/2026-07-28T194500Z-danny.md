# Orchestration Log — Danny

**Agent:** Danny (🏗️ Lead / Architect)
**Mode:** background
**Timestamp:** 2026-07-28T19:45:00Z
**Requested by:** Vincent Huybrechts

## Routing

- **Agent routed:** Danny (Lead / Architect)
- **Why:** Pass 2 (verdict assignment) of the global review, continuing from C1/C2 —
  C6 ("two overlapping 'is it deployed' surfaces exist simultaneously") is a Concept-
  domain item squarely in Danny's remit (CLI surface coherence, deprecation hygiene,
  cross-command naming overlap).
- **Mode:** background — continuing the row-by-row verdict pass on `_lesson.md`
  started in pass 1 and resumed in the C1/C2 round.

## Task

Assign verdicts to `_lesson.md` items C4, C5, and C6, and file an ADR recording the
C6 finding: strata's "deployment status" CLI surface has drifted into four
overlapping/confusing commands, not the two originally suspected.

## Files Produced / Modified

- `docs/decisions/0060-deploy-status-deprecation-and-env-command-clarity.md` (new,
  Status: proposed) — records the C6 finding: `strata deploy status` is deprecated
  but its own deprecation warnings split guidance across two different replacements
  (`env output` for default/live-outputs mode, `deploy plan` for `--plan` mode),
  neither message mentions `env status`; plus confirms `deploy status` has no
  `hidden=True` on its Click registration, so it's fully visible in `--help`
  competing with its replacements. Recommends Option 3: hide `deploy status` from
  `--help` now (no functional removal), fix the stale doc cross-references already
  tracked as I7, and add a doc clarity note distinguishing `env output` (raw
  Terraform outputs) / `env status` (live infra health) / `env show` (resolved
  config). Full removal deferred to a future breaking-change release after 1.5.0.
  Option 4 (consolidate the three `env` commands into one mega-command) explicitly
  rejected — the three commands answer genuinely different questions and merging
  them would be a bigger breaking change for marginal UX gain.
- `_lesson.md` — C6 row updated in place with verdict 🔴 and a note pointing to
  ADR-0060. (C4 and C5 verdicts already present in the file from this round: C4 🟡
  — kind-catalog docs drift across 4 hand-maintained lists, tracked in `_todo.md`;
  C5 🟢 — ADR-0044's dependency-graph/parallel-execution and drift-detection gaps
  re-verified against current code, still accurate.)
- `_todo.md` — new backlog item added: generate "valid kinds" lists from
  `PlatformKind` instead of hand-copying them across 4 docs (from C4).

## Outcome

- **Verified:** ADR-0060 file exists at
  `docs/decisions/0060-deploy-status-deprecation-and-env-command-clarity.md` with
  Status: proposed. `_lesson.md` C4/C5/C6 rows all carry verdicts (🟡/🟢/🔴) with
  ADR-0060 referenced from C6. `_todo.md` carries the C4-derived backlog item.
- **Status:** Complete. C4, C5, and C6 verdicts assigned this round; remaining
  `_lesson.md` rows (D1–D7, I1–I7, T1–T5, X2–X5) still ⏳ for a follow-up pass.
