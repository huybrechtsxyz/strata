# Orchestration Log — Linus

**Agent:** Linus (🐍 Python/CLI Dev)
**Mode:** background
**Timestamp:** 2026-07-28T19:00:00Z
**Requested by:** Vincent Huybrechts

## Routing

- **Agent routed:** Linus (Python/CLI Dev)
- **Why:** Bug fix in `strata new --list` template discovery — Click command / service-layer code, Linus's remit.
- **Mode:** background — surgical bugfix with tests, no design discussion expected.

## Task

`strata new --list` did not show solution-level templates (declared in `solution.json`'s `spec.templates[]`) — it only walked the filesystem (`.strata/templates/` package + workspace dirs). A solution template (e.g. `onboard-customer`) worked fine when run via `strata new <name> <NAME>` but never appeared in `--list` output.

## Files Produced / Modified

- `src/strata/commands/new/run_new_command.py` — hoisted best-effort solution.json loading into new `_load_solution_spec()` method, called before the `--list` early-return; extended `_collect_available_templates()` and `_collect_templates_with_descriptions()` with an optional `solution_templates` parameter merging solution.json templates in as a third source, tagged `type: "bundle (solution)"`, synthesized description `"Solution template: <name> (<N> file(s))"`, last-write-wins on name collision.
- `tests/strata/commands/test_commands_new.py` — added `test_list_includes_solution_level_template` and `test_template_not_found_lists_solution_level_template`.

## Outcome

- **Verified:** ran `tests/strata/commands/test_commands_new.py` — 37 passed, 0 failed. No lint/type errors on either file.
- **Flagged but out of scope:** the "Scaffold bundles (strata sln init --template <name>):" console header text is now technically misleading since solution templates (invoked via `strata new <name> <NAME>`, not `sln init --template`) also land in that section. Left as-is per minimal/surgical fix scope — routed to decisions inbox as an open finding, not actioned.
- **Status:** Complete. Fix implemented, tested, no follow-up code work required this round.
