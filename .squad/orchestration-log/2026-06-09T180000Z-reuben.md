# Orchestration Log — Reuben

**Agent:** Reuben (Docs / Technical Writer)
**Model:** claude-sonnet-4.5
**Mode:** background
**Timestamp:** 2026-06-09T18:00:00Z

## Task

Update `docs/config/dns.md` — add references section, record union table, new "Variable and secret records" section, and updated validation rules.

## Work Completed

### Modified Files (1)

- `docs/config/dns.md`
  - Added `## spec.references Fields` section immediately after `## Top-level Fields`, before `## Zone Fields`
  - Updated Record Fields table: `value`, `var`, `secret` columns now show `one of` in the Required column (instead of `Yes`/`No`); blockquote note appended reinforcing mutual exclusivity
  - Added new `## Variable and secret records` section with:
    - Explanation of the union model and when to use each field
    - Example YAML fragment showing `references:` block + `var:` and `secret:` record usage
    - Note on build-time vs runtime resolution
  - Updated Validation Rules table: added rows for "Exactly one of value/var/secret per record", "var key must be declared in references.variables", "secret key must be declared in references.secrets"

## Design Decisions Applied

- `one of` in Required column (Decision 1 from Reuben's inbox note) — avoids adding a separate Constraints column
- `spec.references` section placed before Zone Fields (Decision 2) — follows natural reading order
- Preserved comprehensive huybrechts.xyz example, modified two TXT records to show `var:` and `secret:` in context (Decision 3)

## Files Changed

- `docs/config/dns.md`
