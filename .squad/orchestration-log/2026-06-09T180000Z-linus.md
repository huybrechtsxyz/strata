# Orchestration Log — Linus

**Agent:** Linus (Python / CLI Dev)
**Model:** claude-sonnet-4.5
**Mode:** background
**Timestamp:** 2026-06-09T18:00:00Z

## Task

DNS record value/var/secret union — `DnsReferencesModel` added, `DnsRecordModel` value/var/secret union, validator for declared references, terraform builder updated for var resolution + secret_records split output.

## Work Completed

### Modified Files (3)

- `src/strata/models/dns_model.py`
  - Added `DnsReferencesModel` with `variables: VariableRefs` and `secrets: SecretRefs` (imported from `common_models`)
  - `DnsRecordModel`: replaced required `value: str` with three optional fields (`value`, `var`, `secret`); added `validate_exactly_one_source` validator (mode="after")
  - `DnsSpecModel`: added `references: Optional[DnsReferencesModel]`; added `validate_references_declared` spec-level validator that checks all used var/secret keys are declared in the references block

- `src/strata/models/platform_artifact_model.py`
  - Added `DnsReferencesModel` import from `dns_model`
  - Added `references: Optional[DnsReferencesModel]` field to `PlatformDnsModel`
  - Updated `from_dns_model()` to pass `references=model.spec.references`

- `src/strata/builders/terraform_builder.py`
  - `_build_dns_vars()`: union dispatch per record — `value:` → literal; `var:` → resolved via `variable_refs` (emits `null` + warning if unresolvable); `secret:` → emits `null` for record value + appends entry to `secret_records` dict
  - Return value extended: `{"dns_zones": ..., "dns_secret_records": ...}`
  - `_save_terraform_vars()`: writes `dns.auto.tfvars.json` (dns_zones only) and new `dns_secret_records.auto.tfvars.json`
  - Added `dns_secret_records.auto.tfvars.json` to dry-run planned file list and `after_build` existence check

## Deviations Documented

- `var:` resolution emits `null` for non-literal stores (azure_key_vault etc.) with a warning message — only `store: literal` variables are fully resolved at build time. This is expected behaviour; full resolution requires runtime secret injection.

## Files Changed

- `src/strata/models/dns_model.py`
- `src/strata/models/platform_artifact_model.py`
- `src/strata/builders/terraform_builder.py`
