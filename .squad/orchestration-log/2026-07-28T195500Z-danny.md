# Orchestration Log — Danny

**Agent:** Danny (🏗️ Lead / Architect)
**Mode:** background
**Timestamp:** 2026-07-28T19:55:00Z
**Requested by:** Vincent Huybrechts

## Routing

- **Agent routed:** Danny (Lead / Architect)
- **Why:** Continuing the row-by-row verdict pass on `_lesson.md` (Design section) —
  D2's subprocess-execution finding needed both a verdict and, given the severity
  (a live hang risk), a filed ADR recording the fix direction before moving to D3+.
- **Mode:** background — verdict pass on `_lesson.md` Design rows D1/D2.

## Task

Assign verdicts to `_lesson.md` items D1 and D2, and file an ADR recording the D2
finding: strata has four independent subprocess execution implementations (not
three as originally framed), with a confirmed missing-timeout bug and a
narrower-than-advertised SIGTERM coverage gap.

## Files Produced / Modified

- `docs/decisions/0061-subprocess-execution-consolidation.md` (new, Status:
  proposed) — records the D2 finding: `run_command()`'s buffered path (default,
  no SIGTERM registration), `run_command()`'s streaming path (has SIGTERM
  registration), `script_deployer.py`'s direct `subprocess.run` (has timeout, no
  SIGTERM registration), and `terraform_builder.py`'s `format=script` builder (NO
  timeout at all — live hang risk). Decision: fix `run_command()`'s buffered path
  first (convert to `Popen` + `register_process`/`deregister_process`, matching
  the streaming path), THEN migrate the other two call sites onto it. The
  `format=script` missing-timeout fix ships independently/immediately as the most
  urgent piece, ahead of the full migration. Option 2 (migrate without fixing the
  buffered path first) explicitly rejected — it would relocate the SIGTERM gap,
  not close it.
- `_lesson.md` — D1 and D2 rows updated in place with verdicts. D1 🟡 —
  `deploy_log_path` genuinely lacks a remote backend, but SIEM forwarding
  (`forward_to_siem()`) already covers off-machine delivery of the same audit
  event, and the gitops manifest (`push_manifest: true`) already covers
  cross-machine deployment-status checks — real gap, low priority, not worth a
  code fix, doc-only follow-up added to `_todo.md`. D2 🔴 — four subprocess
  execution implementations confirmed (not three), `format=script` has no
  timeout at all (live hang risk), `run_command()`'s own buffered/default path
  lacks the SIGTERM registration its streaming path has; ADR-0061 filed with
  fix ordering.
- `_todo.md` — new backlog item added (from D1): document the three
  audit/status delivery mechanisms (local deploy-log, SIEM forwarding, gitops
  manifest) and when to use which — docs-only, no code change needed.

## Outcome

- **Verified:** ADR-0061 file exists at
  `docs/decisions/0061-subprocess-execution-consolidation.md` with Status:
  proposed. `_lesson.md` D1/D2 rows carry verdicts (🟡/🔴) with ADR-0061
  referenced from D2. `_todo.md` carries the D1-derived doc backlog item.
- **Status:** Complete. D1 and D2 verdicts assigned this round; remaining
  `_lesson.md` rows (D3–D7, I1–I7, T1–T5, X2–X5) still ⏳ for a follow-up pass.
