apiVersion: strata.huybrechts.xyz/v1
kind: configuration
meta:
  name: configuration
  annotations:
    description: Standard configuration of the platform
  labels:
    version: "1.0.0"
  tags: []
spec:
  configuration:
    default_build_path: "build/app"
    default_object_path: "build/objects"
    default_dist_path: "build/dist"

  # Policy rules — evaluated at validate, build, plan, and deploy phases
  # policies:
  #   # Enforce tenant zone restrictions on all planned Terraform resources
  #   - name: zone_enforcement
  #     type: tenant_zone
  #     phase: plan
  #     enforcement: deny
  #     description: "Block apply when resources target regions outside tenant-allowed zones"
  #
  #   # Warn when required tags are missing from resources (build phase)
  #   # - name: required_tags
  #   #   type: required_tags
  #   #   phase: build
  #   #   enforcement: warn
  #   #   configuration:
  #   #     required:
  #   #       - environment
  #   #       - owner
