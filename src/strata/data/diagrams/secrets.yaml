apiVersion: strata.huybrechts.xyz/v1
kind: diagram
meta:
  name: secrets
  annotations:
    description: "Declared secrets grouped by environment, coloured by store reachability (offline vs. live) — keys and store types only, never a value"
  labels:
    version: "1.0.0"
  tags:
    - built-in
spec:
  sources:
    - type: secrets
  layout:
    type: flowchart
    direction: LR
  style:
    color_by: status
    group_by: metadata.environment
