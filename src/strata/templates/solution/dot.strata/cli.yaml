# ===============================================================================
# Strata — Workspace CLI Preferences
# ===============================================================================
# Set persistent values for this workspace.
# Use `strata config set <key> <value>` to update from the CLI.
#
# Resolution order (highest to lowest priority):
#   --flag explicitly passed
#   STRATA_* environment variable (STRATA_OUTPUT, STRATA_VERBOSE, STRATA_QUIET, STRATA_WORK_PATH)
#   This file
#   Built-in CLI default
# ===============================================================================

values:
  output: console # console | json | text
  # verbose: false      # set to true to show execution log lines after each command
  # quiet: false        # set to true to suppress all console output
