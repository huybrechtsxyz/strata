apiVersion: strata.huybrechts.xyz/v1
kind: tenant
meta:
  # Missing required 'name' field
  annotations:
    description: Invalid tenant - missing name
spec:
  code: invalid_co
  name: Invalid Co
  zones:
    - eu-west
