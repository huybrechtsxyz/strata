apiVersion: strata.huybrechts.xyz/v1
kind: namespace
meta:
  name: network-broken
  annotations:
    description: This file has the wrong kind — namespace instead of network
  labels:
    version: "1.0.0"
spec:
  networks: []
